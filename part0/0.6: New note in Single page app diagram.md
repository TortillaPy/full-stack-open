sequenceDiagram
    autonumber
    actor User
    participant Browser
    participant Server

    User->>Browser: Types note and clicks "Save"
    
    activate Browser
    Note over Browser: 1. JS event listener intercepts submit<br/>2. Calls e.preventDefault() to stop page reload
    
    Note over Browser: 3. Local UI Update:<br/>- Creates note object locally<br/>- Appends note to DOM (Instant render)<br/>- Clears input field
    
    Browser->>Server: POST /exampleapp/new_note_spa (JSON payload)
    activate Server
    
    Note right of Server: Server-side processing:<br/>- Reads JSON request body<br/>- Creates and saves new note object
    
    Server-->>Browser: 201 Created (JSON confirmation)
    deactivate Server
    
    Note over Browser: 4. Request finished silently<br/>(No page reload or HTML redirection needed)
    deactivate Browser
